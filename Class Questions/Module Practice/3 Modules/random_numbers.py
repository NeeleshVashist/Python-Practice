# Task :
#
# find a random number which can be both positive and negative from the
# specified range entered by the user

import random

def rand_no(a,b):
    print('Random Number from {a} to {b} : ',random.randint(a,b))
